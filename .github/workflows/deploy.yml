name: Deploy Admin Backend to EC2

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Copy SSH key
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > key.pem
          chmod 600 key.pem
      
      - name: Deploy to EC2
        env:
          HOST: ${{ secrets.EC2_HOST }}
          USERNAME: ${{ secrets.EC2_USERNAME }}
        run: |
          ssh -i key.pem -o StrictHostKeyChecking=no $USERNAME@$HOST '
            set -e
            
            cd /home/ubuntu/www/admin
            
            echo "🔐 Fixing permissions"
            sudo chown -R ubuntu:ubuntu /home/ubuntu/www/admin
            sudo chmod -R u+rwX /home/ubuntu/www/admin
            
            echo "⚠️ Backing up .env"
            cp -f .env /tmp/admin-env-backup || true
            
            echo "🧹 Cleaning repo safely"
            git fetch --all
            git reset --hard origin/main
            git clean -fdx -e .env
            
            echo "♻️ Restoring .env"
            cp -f /tmp/admin-env-backup .env || true
            
            echo "📦 Cleaning dependencies"
            rm -rf node_modules dist .prisma
            npm cache clean --force
            
            echo "📦 Installing dependencies"
            npm install --legacy-peer-deps
            
            echo "🔧 Generating Prisma Client"
            npx prisma generate
            
            echo "🏭 Building TypeScript"
            npx tsc
            
            echo "🚀 Restarting backend service"
            pm2 restart admin-backend || pm2 start dist/index.js --name admin-backend
            
            echo "✅ Admin backend deployment completed!"
          '
