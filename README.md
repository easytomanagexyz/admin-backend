# Admin Backend (Eat-with-me)

## Setup

1. Copy `.env.sample` → `.env` and update values.
2. Install deps:
3. Generate prisma client and run migrations:
4. Run dev:
5. Build & start:

Default admin can be created via prisma studio or a seed script.
