# Admin Backend Environment Variables
# Local Development Setup

# Database Configuration
# For local development, use these settings with a local PostgreSQL instance
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/admin_db

# Server Configuration
PORT=4000
NODE_ENV=development

# JWT Configuration
# Generate a random secret for local development
JWT_SECRET=your_jwt_secret_key_change_this_locally

# AWS Configuration (Optional for local development)
# If using local AWS credentials, configure below
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_local_key
# AWS_SECRET_ACCESS_KEY=your_local_secret

# Email Configuration (Optional)
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com

# API URLs
# For local development, point to local instances
FRONTEND_URL=http://localhost:3000
POS_API_URL=http://localhost:5000

# Logging
LOG_LEVEL=debug

# Redis (Optional)
# REDIS_URL=redis://localhost:6379
